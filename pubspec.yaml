name: digital_literacy_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  share_plus:
    path: "D:/flutterprojects/patched_packages/share_plus/share_plus-6.3.4"
  flutter:
    sdk: flutter
  path_provider: ^2.1.5
  # ✅ New dependencies for CSV logging and file storage


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true